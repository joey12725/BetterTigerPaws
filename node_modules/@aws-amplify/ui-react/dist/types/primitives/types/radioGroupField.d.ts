import * as React from 'react';
import { FlexContainerStyleProps } from '@aws-amplify/ui-react/dist/types/primitives/types/flex';
import { FieldProps, LabelPositions } from '@aws-amplify/ui-react/dist/types/primitives/types/field';
import { InputProps } from '@aws-amplify/ui-react/dist/types/primitives/types/input';
export interface RadioGroupFieldProps extends FieldProps, FlexContainerStyleProps, InputProps {
    name: string;
    value?: string;
    defaultValue?: string;
    /**
     * @description
     * Handle onChange event
     */
    onChange?: React.ChangeEventHandler<HTMLInputElement>;
    /**
     * @description
     * Position of label in relation to the radio,
     * default is 'start'
     */
    labelPosition?: LabelPositions;
}
