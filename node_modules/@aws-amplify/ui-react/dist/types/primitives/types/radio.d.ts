import { InputProps } from '@aws-amplify/ui-react/dist/types/primitives/types/input';
import { LabelPositions } from '@aws-amplify/ui-react/dist/types/primitives/types/field';
export interface RadioProps extends InputProps {
    value: string;
    /**
     * @description
     * Position of label in relation to the radio,
     * default is 'start'
     */
    labelPosition?: LabelPositions;
}
